import { GameImage } from '../../models/game-image.model';
import { IBaseService } from './base-service.interface';

export interface IGameImageService extends IBaseService<GameImage> {
  // Métodos específicos si los hay
}
