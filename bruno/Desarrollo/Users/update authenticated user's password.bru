meta {
  name: update authenticated user's password
  type: http
  seq: 4
}

patch {
  url: http://localhost:3000/api/me/password
  body: json
  auth: inherit
}

body:json {
  {
    "currentPassword": "string",
    "newPassword": "stringst"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
