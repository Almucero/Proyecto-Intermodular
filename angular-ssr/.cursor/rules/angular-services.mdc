---
description: Estandares para servicios Angular
globs:
  - "**/*.service.ts"
alwaysApply: false
---

# Angular Services

- Usar `providedIn: 'root'` por defecto.
- Tipar peticiones HTTP con genericos.
- Manejar errores HTTP con `catchError`.
- Preferir cache con `shareReplay` o signals cuando aplique.

```typescript
@Injectable({ providedIn: 'root' })
export class UserService {
  private http = inject(HttpClient);

  getUsers(): Observable<User[]> {
    return this.http.get<User[]>('/api/users').pipe(
      catchError((error) => {
        return throwError(() => error);
      }),
    );
  }
}
```
