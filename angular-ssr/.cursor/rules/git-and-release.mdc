---
description: Convenciones de rama unica, commits y despliegue automatico en Vercel
alwaysApply: true
---

# Git, commits y despliegue

- El proyecto trabaja con rama unica `main`.
- Antes de cerrar un cambio relevante, validar que es desplegable en Vercel.
- El despliegue se ejecuta automaticamente desde Vercel al actualizar `main`.
- Los mensajes de commit deben estar en espanol, con primera letra mayuscula.
- Si hay varios temas relacionados, separar por comas.
- Si hay temas radicalmente distintos, separar con ` + `.
- Si el usuario pide commit, ejecutar antes y en este orden: `npm run build:ssr`, `npm run vercel-build`, `npm run lint`, `npm audit --omit=dev`.
- Si cualquiera de esas validaciones falla, no hacer commit hasta corregir o recibir instruccion explicita del usuario.
