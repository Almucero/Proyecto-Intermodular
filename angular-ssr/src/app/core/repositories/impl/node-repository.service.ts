import { Injectable } from '@angular/core';

/**
 * Servicio de repositorio para Node (actualmente un placeholder).
 * Centraliza la lógica de acceso a datos cuando se utiliza un backend Node.js.
 */
@Injectable({
  providedIn: 'root',
})
export class NodeRepositoryService {
  constructor() {}
}
